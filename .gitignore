# Build artifacts
*.o
*.a
*.pyc
__pycache__/

# Prebuilt kernel is large + extracted from the shipped boot.img (ground truth,
# not our source). Keep it locally; fetch/extract per FINDINGS if cloning fresh.
# Comment out the next line if you want it tracked in the repo.
prebuilt/Image.gz-dtb
