<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Trebuchet gates Home rotation on this being true, and its own default
         is true only under values-sw600dp. This panel is sw480dp, so Home
         could never rotate. The per-user toggle still overrides this. -->
    <bool name="allow_rotation">true</bool>
</resources>
