diff --git a/services/inputflinger/dispatcher/InputDispatcher.cpp b/services/inputflinger/dispatcher/InputDispatcher.cpp
index ab1c132da1..ac35399499 100644
--- a/services/inputflinger/dispatcher/InputDispatcher.cpp
+++ b/services/inputflinger/dispatcher/InputDispatcher.cpp
@@ -433,6 +433,21 @@ status_t InputDispatcher::start() {
     }
     mThread = std::make_unique<InputThread>(
             "InputDispatcher", [this]() { dispatchOnce(); }, [this]() { mLooper->wake(); });
+    // GPD XD+ (patch 0012): dedicated thread that absorbs the blocking power-HAL
+    // poke off the RT dispatcher thread. Its wake callback flags it for exit and
+    // signals the condvar it sleeps on.
+    {
+        std::scoped_lock _l(mUserActivityPokeLock);
+        mUserActivityPokeThreadExiting = false;
+        mUserActivityPokePending = false;
+    }
+    mUserActivityPokeThread = std::make_unique<InputThread>(
+            "InputUserActivity", [this]() { runUserActivityPokeLoop(); },
+            [this]() {
+                std::scoped_lock _l(mUserActivityPokeLock);
+                mUserActivityPokeThreadExiting = true;
+                mUserActivityPokeCond.notify_all();
+            });
     return OK;
 }
 
@@ -441,6 +456,7 @@ status_t InputDispatcher::stop() {
         ALOGE("InputDispatcher cannot be stopped from its own thread!");
         return INVALID_OPERATION;
     }
+    mUserActivityPokeThread.reset();
     mThread.reset();
     return OK;
 }
@@ -476,9 +492,10 @@ void InputDispatcher::dispatchOnce() {
     } // release lock
 
     // Wait for callback or timeout or wake.  (make sure we round up, not down)
-    nsecs_t currentTime = now();
-    int timeoutMillis = toMillisecondTimeoutDelay(currentTime, nextWakeupTime);
-    mLooper->pollOnce(timeoutMillis);
+    // GPD XD+ kernel 3.18.79+ bug: epoll_wait with ANY non-zero timeout
+    // intermittently fails to return. Use pollOnce(0) + usleep to bypass.
+    mLooper->pollOnce(0);
+    usleep(1000);
 }
 
 /**
@@ -2189,6 +2206,7 @@ void InputDispatcher::pokeUserActivityLocked(const EventEntry* eventEntry) {
     }
 
     int32_t eventType = USER_ACTIVITY_EVENT_OTHER;
+    int32_t keyCode = AKEYCODE_UNKNOWN;
     switch (eventEntry->type) {
         case EventEntry::Type::MOTION: {
             const MotionEntry* motionEntry = static_cast<const MotionEntry*>(eventEntry);
@@ -2207,6 +2225,7 @@ void InputDispatcher::pokeUserActivityLocked(const EventEntry* eventEntry) {
                 return;
             }
             eventType = USER_ACTIVITY_EVENT_BUTTON;
+            keyCode = keyEntry->keyCode;
             break;
         }
         case EventEntry::Type::FOCUS:
@@ -2218,16 +2237,48 @@ void InputDispatcher::pokeUserActivityLocked(const EventEntry* eventEntry) {
         }
     }
 
-    std::unique_ptr<CommandEntry> commandEntry =
-            std::make_unique<CommandEntry>(&InputDispatcher::doPokeUserActivityLockedInterruptible);
-    commandEntry->eventTime = eventEntry->eventTime;
-    commandEntry->userActivityEventType = eventType;
-    if (eventType == USER_ACTIVITY_EVENT_BUTTON) {
-        const KeyEntry* keyEntry = static_cast<const KeyEntry*>(eventEntry);
-        commandEntry->keyEntry = const_cast<KeyEntry*>(keyEntry);
-        keyEntry->refCount += 1;
+    // GPD XD+ (patch 0012): hand the poke off to mUserActivityPokeThread instead
+    // of calling mPolicy->pokeUserActivity() on this (RT) InputDispatcher thread.
+    // The poke bottoms out in a synchronous power-HAL binder call
+    // (sendPowerHint INTERACTION) that stalls ~1s on the 3.18.79+ kernel; doing
+    // it inline is the input-lag cause that patch 0014 papered over by dropping
+    // the poke completely (which stopped resetting the screen timeout during
+    // gamepad play). Record the latest request (coalesced) and wake the poke
+    // thread — never blocks dispatch, still keeps the screen awake.
+    {
+        std::scoped_lock pokeLock(mUserActivityPokeLock);
+        mUserActivityPokeEventTime = eventEntry->eventTime;
+        mUserActivityPokeEventType = eventType;
+        mUserActivityPokeKeyCode = keyCode;
+        mUserActivityPokePending = true;
+    }
+    mUserActivityPokeCond.notify_one();
+}
+
+// Thread-safety analysis can't follow the guarded members through the condvar
+// predicate lambda / std::unique_lock, so it is disabled here; mUserActivityPokeLock
+// is held for every access by construction.
+void InputDispatcher::runUserActivityPokeLoop() NO_THREAD_SAFETY_ANALYSIS {
+    nsecs_t eventTime;
+    int32_t eventType;
+    int32_t keyCode;
+    {
+        std::unique_lock<std::mutex> lock(mUserActivityPokeLock);
+        while (!mUserActivityPokePending && !mUserActivityPokeThreadExiting) {
+            mUserActivityPokeCond.wait(lock);
+        }
+        if (mUserActivityPokeThreadExiting) {
+            return;
+        }
+        eventTime = mUserActivityPokeEventTime;
+        eventType = mUserActivityPokeEventType;
+        keyCode = mUserActivityPokeKeyCode;
+        mUserActivityPokePending = false;
     }
-    postCommandLocked(std::move(commandEntry));
+    // Off the InputDispatcher thread: this call absorbs the ~1s power-HAL stall.
+    // PowerManagerService throttles by event type, so bursts collapse to at most
+    // one poke per MIN_TIME_BETWEEN_USERACTIVITIES.
+    mPolicy->pokeUserActivity(eventTime, eventType, keyCode);
 }
 
 void InputDispatcher::prepareDispatchCycleLocked(nsecs_t currentTime,
diff --git a/services/inputflinger/dispatcher/InputDispatcher.h b/services/inputflinger/dispatcher/InputDispatcher.h
index bf93d737fa..60d893820e 100644
--- a/services/inputflinger/dispatcher/InputDispatcher.h
+++ b/services/inputflinger/dispatcher/InputDispatcher.h
@@ -141,6 +141,24 @@ private:
 
     std::unique_ptr<InputThread> mThread;
 
+    // GPD XD+ (patch 0012): pokeUserActivity() ultimately makes a synchronous
+    // power-HAL binder call (sendPowerHint INTERACTION) that blocks ~1s on this
+    // device's 3.18.79+ kernel. Doing it on the RT InputDispatcher thread was the
+    // input-lag cause; patch 0014 stubbed the poke out entirely, which also
+    // dropped the interactive screen-timeout reset (screen slept mid-gamepad
+    // play). Offload the poke to a dedicated thread: the dispatcher never blocks
+    // AND user activity still resets the screen timeout. Requests are coalesced
+    // to the latest.
+    std::unique_ptr<InputThread> mUserActivityPokeThread;
+    std::mutex mUserActivityPokeLock;
+    std::condition_variable mUserActivityPokeCond;
+    bool mUserActivityPokePending GUARDED_BY(mUserActivityPokeLock) = false;
+    bool mUserActivityPokeThreadExiting GUARDED_BY(mUserActivityPokeLock) = false;
+    nsecs_t mUserActivityPokeEventTime GUARDED_BY(mUserActivityPokeLock) = 0;
+    int32_t mUserActivityPokeEventType GUARDED_BY(mUserActivityPokeLock) = 0;
+    int32_t mUserActivityPokeKeyCode GUARDED_BY(mUserActivityPokeLock) = 0;
+    void runUserActivityPokeLoop();
+
     sp<InputDispatcherPolicyInterface> mPolicy;
     android::InputDispatcherConfiguration mConfig;
 
