#
# system.prop for xdplus
#
rild.libpath=mtk-ril.so
rild.libargs=-d /dev/ttyC0


# MTK, Infinity, 20090720 {
wifi.interface=wlan0
ro.hardware=xdplus
# MTK, Infinity, 20090720 }

# MTK, mtk03034, 20101210 {
ro.mediatek.wlan.wsc=1
# MTK, mtk03034 20101210}
# MTK, mtk03034, 20110318 {
ro.mediatek.wlan.p2p=1
# MTK, mtk03034 20110318}

# MTK, mtk03034, 20101213 {
mediatek.wlan.ctia=0
# MTK, mtk03034 20101213}


#
wifi.tethering.interface=ap0
#
ro.sf.lcd_density=240
ro.opengles.version=196610

# Vulkan loader derives the HAL name from this (vulkan.<value>.so); empty here
# meant the loader never found /vendor/lib64/hw/vulkan.mt8173.so (0 phys devices).
ro.hardware.vulkan=mt8173

wifi.direct.interface=p2p0
dalvik.vm.heapgrowthlimit=128m
dalvik.vm.heapsize=256m

# USB MTP WHQL
ro.sys.usb.mtp.whql.enable=0

# Power off opt in IPO
sys.ipo.pwrdncap=2

ro.sys.usb.storage.type=mtp,adb

# USB BICR function
ro.sys.usb.bicr=no

# USB Charge only function
ro.sys.usb.charging.only=yes

# audio
ro.camera.sound.forced=0
ro.audio.silent=0

ro.zygote.preload.enable=0

ro.kernel.zio=38,108,105,16
ro.boot.selinux=disable

debug.hwui.render_dirty_regions=false

ro.mediatek.chip_ver=S01
ro.mediatek.version.release=alps-mp-n0.mp12-V1.9_wisky8176.tb.n_P5
ro.mediatek.platform=MT8173
ro.mediatek.version.sdk=4
ro.telephony.default_network=0
ril.first.md=1
ril.flightmode.poweroffMD=0
ril.telephony.mode=0
persist.mtk.wcn.combo.chipid=-1
ril.specific.sm_cause=0
bgw.current3gband=0
ro.sf.hwrotation=180
ril.radiooff.poweroffMD=0

persist.sys.flat_rotation=1

# LAN OTA server for the LineageOS Updater (docs/OTA_HOSTING.md "Option A").
# HTTPS on a dedicated non-443 port; server cert issued by our CA baked into the
# system trust store (xdplus.mk). App substitutes {device}/{type}/{incr}.
lineage.updater.uri=https://ota.example.com:1443/api/v1/{device}/{type}/{incr}

# MTK gen2 driver mishandles 802.11 power-save: APs low-ack-kick the device
# mid-transfer (PORTING_LOG §44/§49). Patch 0012 pins framework power-save off
# when this is set; init.xdplus.rc covers the raw driver default at load time.
persist.sys.wifi_ps_pin_off=1

# MT6630 gen2 HAL never reports the negotiated PHY mode, so WifiInfo standard
# stays UNKNOWN on every connection. Chip is HT-only on this board (5 GHz/VHT
# eFuse-gated, A/B-proven), so patch 0013 labels the standard 802.11n when this
# is set. Cosmetic (correct UI badge); unset = stock UNKNOWN behavior.
persist.sys.wifi_standard_11n_fallback=1
